$('.button_to').bind('ajax:success', function() {
		$('#status-<%= @attendee_application.id %>').replaceWith('Rejected');
	});


$('.button_to').bind('ajax:success', function() {
		$('#accept-<%= @attendee_application.id %>').empty();
	});

$('.button_to').bind('ajax:success', function() {
		$('#reject-<%= @attendee_application.id %>').empty();
	});
