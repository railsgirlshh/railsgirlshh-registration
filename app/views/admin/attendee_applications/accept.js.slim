| $('.button_to').bind('ajax:success', function() { $('#status-
= @attendee_application.id
| ').replaceWith('Accepted'); }); $('.button_to').bind('ajax:success', function() { $('#accept-
= @attendee_application.id
| ').empty(); }); $('.button_to').bind('ajax:success', function() { $('#reject-
= @attendee_application.id
| ').empty(); }); 
